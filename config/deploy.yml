service: held0-portfolio
image: held0/held0-portfolio

servers:
  web:
    hosts:
      - 91.99.196.182

proxy:
  ssl: true
  hosts:
    - held0.com
    - www.held0.com
  app_port: 3000

registry:
  server: ghcr.io
  username: held0
  password:
    - KAMAL_REGISTRY_PASSWORD

env:
  clear:
    NODE_ENV: production
  secret:
    - SMTP_HOST
    - SMTP_PORT
    - SMTP_USER
    - SMTP_PASS
    - CONTACT_EMAIL

builder:
  arch: amd64
  remote: ssh://deploy@91.99.196.182

ssh:
  user: deploy
